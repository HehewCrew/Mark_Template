# data/examples/posts/ — Confirmed Template Library (flat)

One `<series-slug>-template.md` build sheet per confirmed series template, copied here by the `template-generator` skill after the owner confirms a design. Flat folder — no pillar subfolders.

Each copy carries a canonical-source pointer line at the top; the file under `templates/<pillar>/<slug>/` is the one to edit.

*(empty until the first template is confirmed)*
