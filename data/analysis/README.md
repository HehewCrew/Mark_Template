# data/analysis/ — Analytics Intake (unprocessed)

Drop raw analytics exports (.xls/.xlsx or the platform's export format) here. The `data-analyzer` skill consumes them per `sop/SOP_Data_Analysis.md` and moves processed files to `presentations/data-analysis/<YYYY-MM>/` — this folder holds only unprocessed files.
