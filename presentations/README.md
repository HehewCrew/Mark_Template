# presentations/

- `<YYYY-MM-DD>-<topic-slug>/` — on-demand decks built by the `presentator` skill (self-contained HTML).
- `data-analysis/<YYYY-MM>/` — analytics dashboards + insights produced per `sop/SOP_Data_Analysis.md` (dashboard.html, insights.md, and the source export copied alongside).
